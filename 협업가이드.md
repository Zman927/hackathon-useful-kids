# 협업 가이드 — 유용한 아이들

PM(Jorden)이 정한 개발 순서와 지시사항. **각자 자기 담당 섹션만 보면 됨.**

코드 레벨 상세(테스트 코드, 함수 시그니처 전부)는 아래 두 파일에 있음. 이 문서는 "무슨 순서로, 언제 서로 맞춰야 하는지"만 다룸.
- 백엔드 상세: `docs/superpowers/plans/2026-08-06-backend-api-plan.md`
- 프론트엔드 상세: `docs/superpowers/plans/2026-08-06-frontend-ui-plan.md`

## 팀 역할

| 이름 | 역할 | 담당 배점 | 브랜치 |
|---|---|---|---|
| 박범근 | 백엔드 | 기술 구현 일부 + AI 활용 문서화 | `backend` |
| 허정주 | 프론트엔드 | 기술 구현 30점의 핵심 | `frontend` |
| Jorden | PM · 기획 · 문서 | 문제정의 20 + 실용성 20 + 발표 | `main` (조율), 필요 시 프론트/백엔드 지원 |

## 개발 순서 원칙

- 백엔드가 API 계약(엔드포인트, 요청/응답 형식)을 다 정해놨기 때문에, **프론트는 백엔드를 기다릴 필요 없이 지금 바로 시작 가능.** 화면·폼·상태 관리부터 만들고, 백엔드가 각 엔드포인트를 완성할 때마다 그 부분만 실제 데이터로 바꿔 끼우면 됨.
- API 계약(아래 표)은 **고정.** 바꿔야 하면 반드시 서로에게 알리고 이 문서를 같이 고칠 것 — 한쪽만 조용히 바꾸면 머지할 때 터짐.

### API 계약 (고정)

| 메서드 | 경로 | 용도 |
|---|---|---|
| GET | `/departments` | 학과 목록 |
| GET | `/equipment?department=` | 학과별 기자재 목록 (재고 포함) |
| POST | `/rentals` | 대여 신청 생성 (`equipment_id`, `student_name`, `student_number`, `contact`, `reason`) |
| GET | `/rentals?status=` | 신청 목록 (조교용) |
| PATCH | `/rentals/{id}/approve` | 승인 |
| PATCH | `/rentals/{id}/reject` | 반려 |
| PATCH | `/rentals/{id}/return` | 반납 처리 |

상태값은 한글 고정: `신청됨` / `대여중` / `반려됨` / `반납완료`

---

## 백엔드 개발 순서 — 박범근

**아직 시작 전.** `backend` 브랜치가 초기 상태 그대로임. 오늘 안에 최소 Task 2까지는 끝내야 프론트가 실데이터로 붙을 수 있음 — 최우선 순위로 잡아주세요.

1. **프로젝트 셋업 + 헬스체크** — 완료 기준: `/health` 가 200 응답
2. **기자재 조회 API** (`GET /departments`, `GET /equipment`) — 완료 기준: 시드 데이터 없어도 빈 배열 정상 응답. **이 태스크 끝나면 바로 허정주에게 알릴 것 → 프론트가 목업에서 실데이터로 전환하는 시점**
3. **대여 신청 생성 API** (`POST /rentals`)
4. **대여 목록 + 승인/반려/반납 API** (`GET /rentals`, `PATCH .../approve|reject|return`)
5. **시드 데이터 + 배포** (Docker + Tailscale Funnel, 미니서버) — 완료 기준: 팀원 아무 노트북에서나 배포 URL로 `/health` 확인됨

각 태스크의 정확한 코드·테스트는 `2026-08-06-backend-api-plan.md`의 동일 번호 Task 그대로 따라가면 됨 (TDD: 테스트 먼저 → 실패 확인 → 구현 → 통과 확인 → 커밋).

**AI 활용 역량 20점은 박범근 담당.** 개발 중 Claude Code / Cursor / ChatGPT 중 뭘 어떻게 썼는지 (예: "모델 스키마 설계에 Claude Code로 몇 번 왔다갔다 했다", 실제 프롬프트 몇 개) 오늘부터 기록해두세요 — 나중에 몰아서 쓰려면 기억 안 남음.

---

## 프론트엔드 개발 순서 — 허정주

**이미 라우트 뼈대를 만들어둔 상태** (`Home`, `EquipmentDetail`, `Rental`, `MyPage`). 이 구조를 그대로 쓰고, 원래 계획서(`2026-08-06-frontend-ui-plan.md`)의 `EquipmentList`/`RentalForm`/`AdminDashboard` 이름은 무시하세요 — 이미 만든 걸 기준으로 아래처럼 재정리함.

1. **API 클라이언트 작성** (`src/api.js`) — 백엔드 없어도 지금 시작 가능. 함수만 먼저 만들고, 아래 API 계약대로 각 함수가 뭘 호출하는지만 맞춰두면 됨 (`getDepartments`, `getEquipment`, `createRental`, `getRentals`, `approveRental`, `rejectRental`, `returnRental`)
2. **Home** — 학과 선택 + 기자재 목록. 로딩 상태 / 빈 리스트 안내 포함. 박범근 Task 2 끝나면 실데이터 연결
3. **EquipmentDetail** — 기자재 상세 정보 + "대여 신청" 버튼. 버튼 클릭 시 `navigate('/rental', { state: { equipmentId: item.id } })` 로 이동 (URL에 ID를 안 넣는 지금 라우트 구조를 그대로 살리는 방법)
4. **Rental** — 대여 신청 폼. `useLocation().state.equipmentId`로 어떤 기자재인지 받음. 제출 성공/실패 메시지 필수. 박범근 Task 3 끝나면 연결
5. **조교 대시보드 (신규 라우트 `/admin` 추가 필요)** — 신청 목록 + 승인/반려/반납 버튼. 박범근 Task 4 끝나면 연결. **시간이 부족하면 이 화면은 Jorden이 대신 맡음 — 늦어도 8/6(목) 저녁까지 진행 상황 공유해주세요**
6. **MyPage** — 본인 대여 현황 조회. **Tier 1(여유 있을 때만).** 위 5개가 끝나기 전엔 손대지 마세요 — 지금은 화면만 있는 빈 스텁으로 놔둬도 됨

각 화면은 로딩 상태 / 빈 상태 / 에러 메시지를 반드시 넣을 것 (제출 전 체크리스트 항목, 배점에 직접 영향).

---

## 동기화 체크포인트

| 시점 | 확인할 것 |
|---|---|
| 박범근 Task 2 완료 시 | 즉시 팀 채널에 공유 → 허정주 실데이터 연결 시작 |
| 박범근 Task 3 완료 시 | 즉시 공유 → 허정주 Rental 폼 연결 |
| 박범근 Task 4 완료 시 | 즉시 공유 → 허정주 조교 대시보드(또는 Jorden) 연결 |
| 8/6(목) 저녁 | 셋이 화면 공유로 처음부터 끝까지 한 번 통과하는지 확인 (Notion 허브 원칙: 리뷰 생략, 동작만 확인) |
| 8/6(목) 밤 | **기능 추가 마감(feature freeze).** 이후 버그 수정과 발표 준비만. 정확한 시각은 저녁 체크 후 셋이 그 자리에서 확정 |
| 8/7(금) | 리허설 2회 + 제출 |

---

## 개발 중 프론트-백엔드 통신 (지금 당장)

미니서버·Docker+Funnel은 최종 배포용. **개발 기간 중에는 Tailscale만으로 서로의 로컬 서버에 접속**하면 됨 — 같은 wifi가 아니어도 상관없음.

**박범근(백엔드):**
1. Tailscale 설치 + 팀 tailnet 로그인
2. `backend` 브랜치에서 `docker compose up db -d` (Postgres만 컨테이너로 띄움)
3. `uvicorn app.main:app --reload --port 8000` (API는 로컬에서 직접 — 핫리로드 위해 컨테이너로 안 돌림)
4. `tailscale ip -4`로 본인 IP 확인해서 허정주에게 공유

**허정주(프론트):**
1. Tailscale 설치 + 같은 tailnet 로그인
2. `frontend/.env` 생성 (커밋 안 됨): `VITE_API_BASE=http://<박범근-Tailscale-IP>:8000`

박범근이 노트북을 끄거나 서버를 멈추면 프론트 쪽 API 호출이 전부 실패함 — 버그 아니고 "백엔드 꺼짐" 신호.

## 지금 바로 할 일

- **박범근**: `backend` 브랜치로 체크아웃 → Task 1부터 시작
- **허정주**: 이미 하던 것 계속 → API 클라이언트(`src/api.js`)부터
- **Jorden**: 문제정의·실용성 문서 초안 + 발표 대본 시작, 조교 대시보드 백업 인력으로 대기

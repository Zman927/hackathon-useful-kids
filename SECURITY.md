# Security Policy

---

## 프로젝트 상태

본 프로젝트는 **교내 해커톤 출품작**으로, 현재 운영 환경에 배포되어 있지 않습니다. 아래 내용은 실제 도입 시 반드시 검토해야 할 사항을 포함합니다.

---

## 구현된 보안 조치

| 항목 | 구현 방식 |
|---|---|
| **비밀번호 저장** | bcrypt 해싱. 평문을 저장하지 않음 |
| **인증** | JWT (HS256). 만료 시간 적용 |
| **인가** | 역할 기반 접근 제어(`STUDENT` / `ASSISTANT`) |
| **소유권 검증** | 조교는 **본인 학과** 기자재·대여 신청만 관리 가능 |
| **정보 노출 최소화** | 로그인 실패 시 계정 존재 여부를 구분하지 않는 동일 메시지 반환 |
| **타인 리소스 은닉** | 타인의 대여 신청 접근 시 `403`이 아닌 `404` 반환 |
| **입력 검증** | Pydantic 스키마로 라우터 진입 전 차단 |
| **SQL 인젝션 방지** | SQLAlchemy ORM 파라미터 바인딩 |
| **경쟁 조건 방지** | `SELECT ... FOR UPDATE` 행 잠금 |
| **자격 증명 분리** | `.env`는 커밋 대상에서 제외. `.env.example`만 관리 |

---

## 알려진 제약 사항

현재 구성은 **개발 및 시연 환경을 전제**로 합니다. 다음은 의도적으로 완화된 설정이며, 실제 배포 시 반드시 변경해야 합니다.

### 배포 전 필수 조치

| # | 항목 | 현재 | 필요 조치 |
|---|---|---|---|
| 1 | **`SECRET_KEY`** | `.env.example`에 플레이스홀더 값 | 충분한 엔트로피의 랜덤 값으로 교체. 유출 시 임의 토큰 위조 가능 |
| 2 | **CORS** | `allow_origins=["*"]` | 프론트엔드 도메인으로 제한 |
| 3 | **HTTPS** | HTTP 평문 통신 | TLS 적용. 현재는 Tailscale 사설망에 의존 |
| 4 | **비밀번호 정책** | 시드 계정 공통 `pwd123` | 최소 길이·복잡도 규칙 도입, 초기 비밀번호 변경 강제 |
| 5 | **Rate Limiting** | 없음 | 로그인 엔드포인트에 시도 횟수 제한 (무차별 대입 방어) |
| 6 | **토큰 무효화** | JWT 무상태로 즉시 폐기 불가 | 리프레시 토큰 + 블랙리스트, 또는 짧은 만료 시간 |
| 7 | **파일 업로드 검증** | 확장자 기반 처리 | MIME 타입 검사, 파일 크기 제한, 이미지 재인코딩 |
| 8 | **감사 로그** | 없음 | 승인·반려·반납 등 상태 변경 이력 기록 |

### 설계상 트레이드오프

| 항목 | 설명 |
|---|---|
| **`allow_credentials=False`** | 인증을 쿠키가 아닌 `Authorization` 헤더로 처리합니다. `allow_origins="*"`와 `allow_credentials=True`의 조합은 브라우저 CORS 명세 위반이므로, 현재 조합이 올바른 설정입니다 |
| **권한 판단 시 DB 재조회** | JWT 페이로드에 `role`·`department`가 포함되어 있지만 실제 판단은 DB 조회 결과를 사용합니다. 토큰 발급 후 권한이 변경된 경우를 반영하기 위한 의도적 선택이며, 요청당 1회의 추가 조회 비용이 발생합니다 |

---

## 취약점 신고

보안 취약점을 발견하신 경우, **공개 이슈로 등록하지 말고** 아래 방법으로 알려 주십시오.

- 저장소 관리자([@Zman927](https://github.com/Zman927))에게 직접 연락
- GitHub의 [Private vulnerability reporting](https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability) 기능 이용

신고 시 다음 정보를 포함해 주시면 확인이 빠릅니다.

- 취약점 유형 및 영향 범위
- 재현 절차
- 관련 파일 및 코드 위치

---

## 보안 관련 이력

본 프로젝트는 개발 과정에서 적대적 코드 리뷰를 수행해 다음 결함을 발견·수정했습니다. 상세 내용은 [docs/AI.md — 품질 검증 단계](docs/AI.md#24-품질-검증-단계--가장-큰-성과)에 있습니다.

| 결함 | 조치 |
|---|---|
| CORS 명세 위반 조합 (`origins="*"` + `credentials=True`) | `credentials=False`로 변경 |
| 조교의 타 학과 기자재 등록 가능 | 학과 소유권 검증 추가 |
| 재고 차감 경쟁 조건 | 행 잠금 적용 |
| 로그인 응답과 `/me`의 권한 값 불일치 | 매핑 단일화 |
